spring:
  application:
    name: PrepareService
  main:
    banner-mode: 'off'

  datasource:
    url: jdbc:postgresql://localhost:5433/prepareService
    username: admin
    password: pass
    driver-class-name: org.postgresql.Driver

  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: validate

  liquibase:
    change-log: 'classpath:db/changeLog.xml'

server:
  port: 8802

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  packages-to-scan: com.colvir

management:
  endpoints.web.exposure.include: 'prometheus, metrics, info, health, env'
  endpoint:
    metrics:
      access: read_only
    prometheus:
      access: read_only
  prometheus:
    metrics:
      export:
        enabled: true